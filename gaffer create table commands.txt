cur.execute("CREATE TABLE IF NOT EXISTS 'TEAM'(T_CODE INTEGER PRIMARY KEY, T_NAME VARCHAR(10), 
CAP_NAME VARCHAR(20) NOT NULL, 
USERNAME CHAR(10) NOT NULL, 
PWD VARCHAR(20) NOT NULL)"
)



cur.execute("CREATE TABLE IF NOT EXISTS 'CLIENT'(C_ID INTEGER PRIMARY KEY, C_NAME VARCHAR(20) NOT NULL, 
STREET VARCHAR(15), 
CITY VARCHAR(20) NOT NULL, 
COUNTRY VARCHAR(20), 
EMAIL VARCHAR(30))"
)



cur.execute("CREATE TABLE IF NOT EXISTS 'MEMBERS'(M_ID CHAR(10) PRIMARY KEY,
AGE INTEGER,
M_NAME VARCHAR(20) NOT NULL, 
CELL INTEGER UNIQUE, 
COUNTRY VARCHAR(15),
START_DATE DATE, 
TEAM_CODE INTEGER, 
ROLE_ID INTEGER, 
CONSTRAINT FK3 FOREIGN KEY (TEAM_CODE) REFERENCES TEAM (T_CODE) ON DELETE CASCADE ON UPDATE CASCADE, 
CONSTRAINT FK5 FOREIGN KEY(ROLE_ID) REFERENCES ROLE(R_ID) ON DELETE CASCADE ON UPDATE CASCADE)")



cur.execute("CREATE TABLE IF NOT EXISTS 'ROLE'(R_ID INTEGER PRIMARY KEY, 
R_NAME CHAR(15) NOT NULL, 
SALARY INTEGER)"
)



cur.execute("CREATE TABLE IF NOT EXISTS 'CLIENT_MOIBLE'(MOBILE_ID INTEGER PRIMARY KEY AUTOINCREMENT, 
MOBILE BIGINT,
CUSTOMER_ID INTEGER, 
CONSTRAINT FK1 FOREIGN KEY (CUSTOMER_ID) REFERENCES CLIENT (C_ID) ON DELETE CASCADE  ON UPDATE CASCADE)")



cur.execute("CREATE TABLE IF NOT EXISTS 'PROJECT'(P_CODE INTEGER PRIMARY KEY,
P_NAME VARCHAR(15) NOT NULL, 
BUDGET INTEGER, 
STATUS VARCHAR(8), 
START_DATE DATE NOT NULL, 
DUE_DATE DATE NOT NULL, 
CUST_ID INTEGER, 
CONSTRAINT FK2 FOREIGN KEY (CUST_ID) REFERENCES CLIENT (C_ID) ON DELETE CASCADE ON UPDATE CASCADE)")





cur.execute("CREATE TABLE 'WORKON' (MEM_ID CHAR(10),PROJ_CODE INTEGER,CONSTRAINT FK6 FOREIGN KEY (PROJ_CODE) REFERENCES PROJECT (P_CODE) ON DELETE CASCADE ON UPDATE CASCADE,CONSTRAINT FK7 FOREIGN KEY (MEM_ID) REFERENCES MEMBER (M_ID) ON DELETE CASCADE ON UPDATE CASCADE)")

